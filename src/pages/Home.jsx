import { Banner, Card, Nav } from '../paths';

const Home = () => {
    return (
        <div>
            <Nav />
            <Banner />
            <Card />
        </div>
    );
};

export default Home;
